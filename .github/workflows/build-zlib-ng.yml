name: Build zlib-ng static libraries

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - zlib-ng/**
      - .github/workflows/build-zlib-ng.yml
  pull_request:
    branches: [ master ]
    paths:
      - zlib-ng/**
      - .github/workflows/build-zlib-ng.yml

env:
  ZLIB_NG_VERSION: 2.1.6
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-linux:
    name: Build ${{ matrix.platform_arch }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: linux-amd64
            runner: ubuntu-22.04
            docker_platform: linux/amd64
            platform_arch: linux/x86_64
            use_qemu: false
          - project: linux-arm32v7
            runner: ubuntu-22.04
            docker_platform: linux/arm/v7
            platform_arch: linux/armv7l
            use_qemu: true
          - project: linux-arm64
            runner: ubuntu-22.04-arm
            docker_platform: linux/arm64
            platform_arch: linux/aarch64
            use_qemu: false

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Allow multiarch
        if: ${{ matrix.use_qemu }}
        run: docker run --rm --privileged multiarch/qemu-user-static --reset -p yes

      - name: Build static libraries
        run: |
          docker run \
            --platform ${{ matrix.docker_platform }} \
            -v "${PWD}:/sources" \
            -w /sources \
            -t totalcross/${{ matrix.project }}:v1.0.7 \
            bash -lc "
              cmake --version &&
              cmake -S /sources/zlib-ng -B /sources/zlib-ng/build/${{ matrix.project }} \
                -DCMAKE_BUILD_TYPE=Release \
                -G Ninja &&
              cmake --build /sources/zlib-ng/build/${{ matrix.project }} &&
              cmake --install /sources/zlib-ng/build/${{ matrix.project }} --prefix /sources/zlib-ng/build/${{ matrix.project }}/install &&
              bash /sources/zlib-ng/scripts/package-artifact.sh \
                /sources/zlib-ng/build/${{ matrix.project }} \
                /sources/zlib-ng/build/${{ matrix.project }}/install \
                ${{ matrix.platform_arch }}
            "

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: zlib-ng-${{ matrix.project }}
          path: zlib-ng/build/${{ matrix.project }}/zlib-ng-*.tar.gz

  build-windows:
    name: Build windows/x86
    runs-on: windows-2022

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Build static libraries
        shell: bash
        run: |
          cmake -S zlib-ng -B zlib-ng/build/windows-x86 \
            -DCMAKE_BUILD_TYPE=Release \
            -G "Visual Studio 17 2022" \
            -A Win32
          cmake --build zlib-ng/build/windows-x86 --config Release
          cmake --install zlib-ng/build/windows-x86 --config Release --prefix zlib-ng/build/windows-x86/install
          bash zlib-ng/scripts/package-artifact.sh zlib-ng/build/windows-x86 zlib-ng/build/windows-x86/install windows/x86

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: zlib-ng-windows-x86
          path: zlib-ng/build/windows-x86/zlib-ng-*.tar.gz

  build-android:
    name: Build android/arm64-v8a
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: ''

      - name: Install Android NDK
        run: sdkmanager "ndk;28.2.13676358" "cmake;3.22.1"

      - name: Build static libraries
        run: |
          cmake -S zlib-ng -B zlib-ng/build/android-arm64-v8a \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE="${ANDROID_HOME}/ndk/28.2.13676358/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-23 \
            -DANDROID_USE_LEGACY_TOOLCHAIN_FILE=OFF \
            -G Ninja
          cmake --build zlib-ng/build/android-arm64-v8a
          cmake --install zlib-ng/build/android-arm64-v8a --prefix zlib-ng/build/android-arm64-v8a/install
          bash zlib-ng/scripts/package-artifact.sh zlib-ng/build/android-arm64-v8a zlib-ng/build/android-arm64-v8a/install android/arm64-v8a

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: zlib-ng-android-arm64-v8a
          path: zlib-ng/build/android-arm64-v8a/zlib-ng-*.tar.gz

  build-ios:
    name: Build ios/arm64
    runs-on: macos-15

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Build static libraries
        run: |
          cmake -S zlib-ng -B zlib-ng/build/ios-arm64 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGNING_ALLOWED=NO \
            -G Xcode
          cmake --build zlib-ng/build/ios-arm64 --config Release
          cmake --install zlib-ng/build/ios-arm64 --config Release --prefix zlib-ng/build/ios-arm64/install
          bash zlib-ng/scripts/package-artifact.sh zlib-ng/build/ios-arm64 zlib-ng/build/ios-arm64/install ios/arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: zlib-ng-ios-arm64
          path: zlib-ng/build/ios-arm64/zlib-ng-*.tar.gz

  build-macos:
    name: Build macos/arm64
    runs-on: macos-15

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Build static libraries
        run: |
          cmake -S zlib-ng -B zlib-ng/build/macos-arm64 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -G Ninja
          cmake --build zlib-ng/build/macos-arm64
          cmake --install zlib-ng/build/macos-arm64 --prefix zlib-ng/build/macos-arm64/install
          bash zlib-ng/scripts/package-artifact.sh zlib-ng/build/macos-arm64 zlib-ng/build/macos-arm64/install macos/arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: zlib-ng-macos-arm64
          path: zlib-ng/build/macos-arm64/zlib-ng-*.tar.gz
