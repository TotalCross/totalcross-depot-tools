name: Build mbedTLS static libraries

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - mbedtls/**
      - .github/workflows/build-mbedtls.yml
  pull_request:
    branches: [ master ]
    paths:
      - mbedtls/**
      - .github/workflows/build-mbedtls.yml

env:
  MBEDTLS_VERSION: 3.5.2
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-linux:
    name: Build ${{ matrix.platform_arch }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: linux-amd64
            runner: ubuntu-22.04
            docker_platform: linux/amd64
            platform_arch: linux/x86_64
            use_qemu: false
          - project: linux-arm32v7
            runner: ubuntu-22.04
            docker_platform: linux/arm/v7
            platform_arch: linux/armv7l
            use_qemu: true
          - project: linux-arm64
            runner: ubuntu-22.04-arm
            docker_platform: linux/arm64
            platform_arch: linux/aarch64
            use_qemu: false

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Allow multiarch
        if: ${{ matrix.use_qemu }}
        run: docker run --rm --privileged multiarch/qemu-user-static --reset -p yes

      - name: Build static libraries
        run: |
          docker run \
            --platform ${{ matrix.docker_platform }} \
            -v "${PWD}:/sources" \
            -w /sources \
            -t totalcross/${{ matrix.project }}:v1.0.7 \
            bash -lc "
              cmake --version &&
              cmake -S /sources/mbedtls -B /sources/mbedtls/build/${{ matrix.project }} \
                -DCMAKE_BUILD_TYPE=Release \
                -G Ninja &&
              cmake --build /sources/mbedtls/build/${{ matrix.project }} &&
              cmake --install /sources/mbedtls/build/${{ matrix.project }} --prefix /sources/mbedtls/build/${{ matrix.project }}/install &&
              bash /sources/mbedtls/scripts/package-artifact.sh \
                /sources/mbedtls/build/${{ matrix.project }} \
                /sources/mbedtls/build/${{ matrix.project }}/install \
                ${{ matrix.platform_arch }}
            "

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mbedtls-${{ matrix.project }}
          path: mbedtls/build/${{ matrix.project }}/mbedtls-*.tar.gz

  build-windows:
    name: Build windows/x86
    runs-on: windows-2022

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Build static libraries
        shell: bash
        run: |
          cmake -S mbedtls -B mbedtls/build/windows-x86 \
            -DCMAKE_BUILD_TYPE=Release \
            -G "Visual Studio 17 2022" \
            -A Win32
          cmake --build mbedtls/build/windows-x86 --config Release
          cmake --install mbedtls/build/windows-x86 --config Release --prefix mbedtls/build/windows-x86/install
          bash mbedtls/scripts/package-artifact.sh mbedtls/build/windows-x86 mbedtls/build/windows-x86/install windows/x86

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mbedtls-windows-x86
          path: mbedtls/build/windows-x86/mbedtls-*.tar.gz

  build-android:
    name: Build android/arm64-v8a
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: ''

      - name: Install Android NDK
        run: sdkmanager "ndk;28.2.13676358" "cmake;3.22.1"

      - name: Build static libraries
        run: |
          cmake -S mbedtls -B mbedtls/build/android-arm64-v8a \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_TOOLCHAIN_FILE="${ANDROID_HOME}/ndk/28.2.13676358/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-23 \
            -DANDROID_USE_LEGACY_TOOLCHAIN_FILE=OFF \
            -G Ninja
          cmake --build mbedtls/build/android-arm64-v8a
          cmake --install mbedtls/build/android-arm64-v8a --prefix mbedtls/build/android-arm64-v8a/install
          bash mbedtls/scripts/package-artifact.sh mbedtls/build/android-arm64-v8a mbedtls/build/android-arm64-v8a/install android/arm64-v8a

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mbedtls-android-arm64-v8a
          path: mbedtls/build/android-arm64-v8a/mbedtls-*.tar.gz

  build-ios:
    name: Build ios/arm64
    runs-on: macos-15

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Build static libraries
        run: |
          cmake -S mbedtls -B mbedtls/build/ios-arm64 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_SYSTEM_NAME=iOS \
            -DCMAKE_OSX_SYSROOT=iphoneos \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_XCODE_ATTRIBUTE_CODE_SIGNING_ALLOWED=NO \
            -G Xcode
          cmake --build mbedtls/build/ios-arm64 --config Release
          cmake --install mbedtls/build/ios-arm64 --config Release --prefix mbedtls/build/ios-arm64/install
          bash mbedtls/scripts/package-artifact.sh mbedtls/build/ios-arm64 mbedtls/build/ios-arm64/install ios/arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mbedtls-ios-arm64
          path: mbedtls/build/ios-arm64/mbedtls-*.tar.gz

  build-macos:
    name: Build macos/arm64
    runs-on: macos-15

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Build static libraries
        run: |
          cmake -S mbedtls -B mbedtls/build/macos-arm64 \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -G Ninja
          cmake --build mbedtls/build/macos-arm64
          cmake --install mbedtls/build/macos-arm64 --prefix mbedtls/build/macos-arm64/install
          bash mbedtls/scripts/package-artifact.sh mbedtls/build/macos-arm64 mbedtls/build/macos-arm64/install macos/arm64

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mbedtls-macos-arm64
          path: mbedtls/build/macos-arm64/mbedtls-*.tar.gz
