# SPDX-FileCopyrightText: 2026 Amalgam Solucoes em TI Ltda.
# SPDX-License-Identifier: MIT
name: Release zlib-ng static libraries

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: Validate assets without changing pins, tags, or releases
        required: true
        default: true
        type: boolean

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  prepare:
    name: Prepare release
    runs-on: ubuntu-24.04
    outputs:
      zlib_ng_version: ${{ steps.version.outputs.zlib_ng_version }}
      tag_name: ${{ steps.version.outputs.tag_name }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Read zlib-ng version
        id: version
        run: |
          zlib_ng_version="$(awk '/^[[:space:]]*VERSION[[:space:]]+[0-9]/{print $2; exit}' zlib-ng/CMakeLists.txt)"
          if [ -z "$zlib_ng_version" ]; then
            echo "Could not read zlib-ng version from zlib-ng/CMakeLists.txt" >&2
            exit 1
          fi

          tag_name="$(.github/scripts/next-release-tag.sh zlib-ng "${zlib_ng_version}")"

          echo "zlib_ng_version=${zlib_ng_version}" >> "$GITHUB_OUTPUT"
          echo "tag_name=${tag_name}" >> "$GITHUB_OUTPUT"

  build:
    name: Build release assets
    needs: prepare
    uses: ./.github/workflows/build-zlib-ng.yml

  release:
    name: Publish release
    needs:
      - prepare
      - build
    runs-on: ubuntu-24.04
    env:
      GH_TOKEN: ${{ github.token }}
      ZLIB_NG_VERSION: ${{ needs.prepare.outputs.zlib_ng_version }}
      TAG_NAME: ${{ needs.prepare.outputs.tag_name }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          path: release-assets
          merge-multiple: true

      - name: Pin bundle release
        if: ${{ !inputs.dry_run }}
        id: pin_bundle_release
        uses: ./.github/actions/pin-deps-release
        with:
          dependency: zlib-ng
          tag: ${{ env.TAG_NAME }}

      - name: Create release tag
        if: ${{ !inputs.dry_run }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${TAG_NAME}" -m "zlib-ng ${ZLIB_NG_VERSION}"
          git push origin "refs/tags/${TAG_NAME}"

      - name: Publish GitHub Release
        if: ${{ !inputs.dry_run }}
        run: |
          gh release create "${TAG_NAME}" release-assets/* \
            --verify-tag \
            --title "zlib-ng ${ZLIB_NG_VERSION} static libraries" \
            --notes "Static zlib-ng ${ZLIB_NG_VERSION} libraries for TotalCross."

      - name: Revert bundle release pin after failure
        if: ${{ failure() && steps.pin_bundle_release.outputs.commit != '' }}
        uses: ./.github/actions/revert-deps-release-pin
        with:
          commit: ${{ steps.pin_bundle_release.outputs.commit }}
